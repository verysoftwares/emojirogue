function love.load()
    -- emoji font source:
    -- https://dn-works.com/ufas/
    -- for personal use only
    -- emojifon= lg.newFont('wares/Symbola.otf', 48)
    emojifon= lg.newFont('wares/EmojiOneColor.otf', 64)
    hoverfon= lg.newFont('wares/FreePixel.ttf', 16)
    lg.setFont(emojifon)
end